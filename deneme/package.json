{
  "name": "kaptanin-seyahatleri",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client"
  },
  "workspaces": [
    "server",
    "client"
  ]
}